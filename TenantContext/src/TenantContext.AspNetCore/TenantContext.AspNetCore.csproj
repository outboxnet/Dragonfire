<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>TenantContext.AspNetCore</RootNamespace>
    <AssemblyName>TenantContext.AspNetCore</AssemblyName>
    <PackageId>TenantContext.AspNetCore</PackageId>
    <Description>ASP.NET Core integration for TenantContext: middleware, header/subdomain/claim/route/query resolvers. Pulls tenant from HTTP, applies resolution policy, and sets the AsyncLocal scope for the duration of the request.</Description>
  </PropertyGroup>
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\TenantContext\TenantContext.csproj" />
  </ItemGroup>
</Project>
