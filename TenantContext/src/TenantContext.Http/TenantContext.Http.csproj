<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>TenantContext.Http</RootNamespace>
    <AssemblyName>TenantContext.Http</AssemblyName>
    <PackageId>TenantContext.Http</PackageId>
    <Description>Outbound HttpClient propagation for TenantContext: a DelegatingHandler that stamps the current tenant onto outgoing HTTP requests so downstream services receive it on the configured header.</Description>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Http" Version="8.0.1" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\TenantContext\TenantContext.csproj" />
  </ItemGroup>
</Project>
