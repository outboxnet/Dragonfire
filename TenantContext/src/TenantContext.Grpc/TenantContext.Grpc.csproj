<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>TenantContext.Grpc</RootNamespace>
    <AssemblyName>TenantContext.Grpc</AssemblyName>
    <PackageId>TenantContext.Grpc</PackageId>
    <Description>gRPC interceptors for TenantContext: outbound client interceptor stamps the tenant id into request metadata; server-side resolver reads it back and pushes it into the AsyncLocal scope.</Description>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Grpc.Core.Api" Version="2.65.0" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\TenantContext\TenantContext.csproj" />
  </ItemGroup>
</Project>
